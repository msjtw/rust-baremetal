[package]
name = "rust-baremetal"
version = "0.1.0"
edition = "2024"

[dependencies]
allocator = {path = "../allocator"}

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[[bin]]
name = "main"
path = "src/main.rs"
test = false
bench = false
